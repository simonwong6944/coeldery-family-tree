# API Keys / Secrets — 憲法級：Key 永不入 git
.env
.env.*
!.env.example

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Wrangler local state (本機 D1 SQLite，不入 git)
.wrangler/

# D1 資料備份 dump（含真實資料，不入 git）
backup-*.sql

# 臨時編譯輸出（測試用）
.tmp-kin/
.tmp-gather/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
